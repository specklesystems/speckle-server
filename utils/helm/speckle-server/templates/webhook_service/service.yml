apiVersion: v1
kind: Service
metadata:
  name: speckle-webhook-service-metrics
  namespace: {{ .Values.namespace }}
  labels:
{{ include "webhook_service.labels" . | indent 4 }}
spec:
  selector:
{{ include "webhook_service.selectorLabels" . | indent 4 }}
  ports:
    - name: web
      port: 9095
      protocol: TCP
      targetPort: metrics
