apiVersion: v1
kind: Service
metadata:
  name: speckle-frontend
  namespace: {{ .Values.namespace }}
  labels:
    app: speckle-frontend
    project: speckle-server
spec:
  selector:
    app: speckle-frontend
    project: speckle-server
  ports:
    - protocol: TCP
      name: www
      port: 80
      targetPort: www
