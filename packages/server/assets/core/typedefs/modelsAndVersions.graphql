extend type Project {
  models(cursor: String, limit: Int! = 25): ModelCollection
}

type Model {
  id: ID!
  name: String!
  versionCount: Int!
  author: LimitedUser!
  sourceApp: String!
}

type ModelCollection {
  totalCount: Int!
  cursor: String
  items: [Model!]!
}
