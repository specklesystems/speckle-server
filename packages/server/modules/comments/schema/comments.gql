
extend type Mutation {
  userCommentActivityBroadcast( streamId: String!, resourceId: String!, data: JSONObject) : Boolean
    @hasRole(role: "server:user")
    @hasScope(role: "stream:read")
}

extend type Subscription {
  userCommentActivity(streamId: String!, resourceId: String!)
    @hasRole(role: "server:user")
    @hasScope(role: "stream:read")
}