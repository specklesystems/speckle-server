describe('Workspace join services', () => {
  describe('joinWorkspaceFactory returns a function, that', () => {
    it('throws an error if the workspace is not discoverable')
    it('throws an error if the workspace has no verified domains')
    it('throws an error if the user has no verified email matching the domains')
    it('creates a workspace member role and emits workspace events')
  })
})
