export enum GridListToggleValue {
  Grid = 'grid',
  List = 'list'
}

export type LayoutTabItem = {
  title: string
  id: string
}

export type LayoutMenuItem = {
  title: string
  id: string
  disabled?: boolean
}
